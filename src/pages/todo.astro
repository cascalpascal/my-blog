---
// src/pages/todo.astro
// 대시보드와 동일한 레이아웃 구조를 가집니다.
---

<!doctype html>
<html lang="ko">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>학습 일정 관리</title>
		<link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.3.0/flowbite.min.css" rel="stylesheet" />
	</head>
	<body class="bg-[#fafafa] text-gray-800 antialiased min-h-screen flex flex-col">

		<nav class="fixed top-0 z-50 w-full bg-white border-b border-gray-100 h-16 flex items-center justify-between px-6">
			<div class="flex items-center gap-3">
				<button id="toggle-sidebar-btn" type="button" class="inline-flex items-center p-2 text-sm text-gray-500 rounded-lg hover:bg-gray-100 focus:outline-none">
					<svg class="w-6 h-6" aria-hidden="true" fill="currentColor" viewBox="0 0 20 20">
						<path clip-rule="evenodd" fill-rule="evenodd" d="M2 4.75A.75.75 0 012.75 4h14.5a.75.75 0 010 1.5H2.75A.75.75 0 012 4.75zm0 10.5a.75.75 0 01.75-.75h14.5a.75.75 0 010 1.5H2.75a.75.75 0 01-.75-.75zm0-5.25a.75.75 0 01.75-.75h14.5a.75.75 0 010 1.5H2.75a.75.75 0 01-.75-.75z"></path>
					</svg>
				</button>
			</div>
			<div class="text-xs font-semibold text-gray-400">📅 현재 시간 연동 중</div>
		</nav>

		<div class="flex flex-1 pt-16 h-[calc(100vh-4rem)] overflow-hidden">
			<aside id="main-sidebar" class="w-16 h-full bg-white border-r border-gray-100 overflow-y-auto overflow-x-hidden flex-shrink-0 transition-all duration-300">
				<div class="h-full px-2 py-4 flex flex-col justify-between">
					<div>
						<div id="sidebar-profile" class="hidden flex items-center gap-3 px-2 mb-6 transition-opacity duration-200">
							<div class="w-9 h-9 min-w-[36px] rounded-full bg-amber-100 flex items-center justify-center font-bold text-amber-700 text-sm">메뉴</div>
						</div>
						<ul class="space-y-2 font-medium text-xs">
							<li><a href="/" class="flex items-center justify-center p-2.5 text-gray-600 rounded-xl hover:bg-gray-50 sidebar-link" title="소개"><span class="text-base min-w-[28px] text-center">👤</span><span class="sidebar-text hidden ml-2 whitespace-nowrap opacity-0 transition-all duration-200">소개</span></a></li>
							<li><a href="/todo" class="flex items-center justify-center p-2.5 text-amber-600 rounded-xl bg-amber-50/60 font-semibold sidebar-link" title="학습 일정"><span class="text-base min-w-[28px] text-center">📅</span><span class="sidebar-text hidden ml-2 whitespace-nowrap opacity-0 transition-all duration-200">학습 일정</span></a></li>
							<li><a href="/blog" class="flex items-center justify-center p-2.5 text-gray-600 rounded-xl hover:bg-gray-50 sidebar-link" title="필기"><span class="text-base min-w-[28px] text-center">📝</span><span class="sidebar-text hidden ml-2 whitespace-nowrap opacity-0 transition-all duration-200">필기</span></a></li>
							<li><a href="/wrong-notes" class="flex items-center justify-center p-2.5 text-gray-600 rounded-xl hover:bg-gray-50 sidebar-link" title="오답노트"><span class="text-base min-w-[28px] text-center">📕</span><span class="sidebar-text hidden ml-2 whitespace-nowrap opacity-0 transition-all duration-200">오답노트</span></a></li>
						</ul>
					</div>
					<div class="text-[9px] text-gray-400 sidebar-text hidden opacity-0 whitespace-nowrap text-center">© 2026</div>
				</div>
			</aside>

			<main class="flex-1 p-6 h-full overflow-y-auto">
				<div class="max-w-4xl mx-auto space-y-6">
					<h1 class="text-xl font-bold text-gray-800">📅 학습 일정 및 목표 설정</h1>
					<div class="bg-white p-6 rounded-2xl border border-gray-100 shadow-sm" style="display: block !important;">
                        <h3 class="text-sm font-bold text-gray-700 mb-3">🎯 목표 시험일 설정 (D-day 자동 연동)</h3>
                        
                        <div style="display: flex; flex-direction: row; align-items: center; gap: 12px; margin-top: 10px;">
                            <input 
                                type="date" 
                                id="exam-date-input" 
                                value="2026-07-20" 
                                class="bg-gray-50 border border-gray-200 text-gray-900 text-sm rounded-xl focus:ring-amber-500 focus:border-amber-500 p-2.5"
                                style="height: 42px; px: 10px;"
                            >
                            
                            <button 
                                type="button"
                                id="save-date-btn" 
                                class="text-white text-xs font-bold rounded-xl"
                                style="background-color: #f59e0b !important; color: white !important; display: inline-block !important; visibility: visible !important; height: 42px; padding: 0 20px; cursor: pointer; border: none; font-weight: bold;"
                            >
                                설정 저장
                            </button>
                        </div>
                        
                        <p class="text-xs text-gray-400 mt-3">여기에 지정한 날짜를 기준으로 홈 화면의 D-day 카운트다운이 실시간 계산됩니다.</p>
                    </div>

				</div>
			</main>
		</div>

		<script>
			// 사이드바 토글 로직
			const toggleBtn = document.getElementById('toggle-sidebar-btn');
			const sidebar = document.getElementById('main-sidebar');
			const profile = document.getElementById('sidebar-profile');
			const texts = document.querySelectorAll('.sidebar-text');
			const links = document.querySelectorAll('.sidebar-link');

			toggleBtn?.addEventListener('click', () => {
				sidebar?.classList.toggle('w-16');
				sidebar?.classList.toggle('w-64');
				profile?.classList.toggle('hidden');
				texts.forEach(text => {
					text.classList.toggle('hidden');
					setTimeout(() => { text.classList.toggle('opacity-0'); }, 50);
				});
				links.forEach(link => {
					link.classList.toggle('justify-center');
					link.classList.toggle('justify-start');
				});
			});
            // 요소 가져오기 및 타입 지정 확실히 해두기
	        const dateInput = document.getElementById('exam-date-input') as HTMLInputElement | null;
            const saveBtn = document.getElementById('save-date-btn');

            // 기존 저장된 날짜 불러오기
            if (dateInput && localStorage.getItem('targetExamDate')) {
                dateInput.value = localStorage.getItem('targetExamDate')!;
            }

            // 저장 버튼 이벤트 리스너 연결
            saveBtn?.addEventListener('click', () => {
                if (dateInput) {
                    localStorage.setItem('targetExamDate', dateInput.value);
                    alert('🎯 시험 일정이 브라우저에 안전하게 저장되었습니다! 홈 화면으로 가서 D-day를 확인해 보세요.');
                }
            });
        </script>
	</body>
</html>